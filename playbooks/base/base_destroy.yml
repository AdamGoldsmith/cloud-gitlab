---

- name: Cloud provider base configuration destruction
  hosts: localhost
  connection: local
  gather_facts: no
  tags:
    - never
    - destroy
    - base_destroy

  tasks:

    # Include tasks for writing terraform vars to file as workaround for
    # https://github.com/ansible/ansible/issues/51687
    # TODO: Remove this task and the included_tasks file when
    # issue is fixed
    - name: "Include terraform tasks"
      vars:
        service_name: base
        tf_state: absent
        tf_vars:
          region_name: "{{ project_region[cloud_provider] }}"
          network_name: "{{ project_data['network']['name'] }}"
        tf_vars_all: "{{ tf_cred_data[cloud_provider] |
          combine(tf_vars) }}"
      include_tasks:
        file: "{{ inventory_dir |
          default(ansible_inventory_sources[0])
          }}/../tasks/terraform.yml"

    # TODO: Uncomment task when passing variables to terraform issue is fixed
    # https://github.com/ansible/ansible/issues/51687
    # - name: Destroy base cloud configuration resources
    #   vars:
    #     service_name: base
    #     tf_vars:
    #       region_name: "{{ project_region[cloud_provider] }}"
    #       network_name: "{{ project_data['network']['name'] }}"
    #     tf_vars_all: "{{ tf_cred_data[cloud_provider] |
    #       combine(tf_vars) }}"
    #   terraform:
    #     project_path: "{{ inventory_dir |
    #       default(ansible_inventory_sources[0])
    #       }}/../terraform/{{ cloud_provider }}/{{ service_name }}"
    #     state: absent
    #     lock: yes
    #     variables: "{{ tf_vars_all }}"
    #     backend_config: "{{ tf_backend_config[cloud_provider] }}"

      when:
        - cloud_provider is defined
        - cloud_provider == 'gcp'

  post_tasks:

    # TODO: Requires some work here because bucket is not empty on destroy
    # # Use Ansible to destroy bucket for storing terraform remote state files.
    # - name: Destroy terraform backend storage bucket
    #   gcp_storage_bucket:
    #     name: "{{ cred_data[cloud_provider]['project_name'] }}-tfstate"
    #     location: "{{ project_region[cloud_provider] }}"
    #     versioning:
    #       enabled: yes
    #     project: "{{ cred_data[cloud_provider]['project_name'] }}"
    #     auth_kind: "{{ cred_data[cloud_provider]['auth_kind'] }}"
    #     service_account_file: "{{ cred_data[cloud_provider]['cred_file'] }}"
    #     state: absent

    # - pause:
