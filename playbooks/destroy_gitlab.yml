---

- name: Cloud provider deployment
  hosts: localhost
  connection: local
  gather_facts: no
  tags:
    - never
    - destroy

  tasks:

    - name: Destroy cloud instance
      include_role:
        name: ansible-role-destroy-gitlab

