---

- name: Cloud provider deployment
  hosts: localhost
  connection: local
  gather_facts: no
  tags:
    - never
    - delete

  roles:

    - name: Destroy cloud instance
      role: ansible-role-delete-instance

