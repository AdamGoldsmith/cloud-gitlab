---

- name: Manage site configuration
  hosts: localhost
  connection: local
  gather_facts: no
  tags:
    - always

  pre_tasks:

    - name: Include project-wide variables
      include_vars:
        file: ../vars/main.yml

- import_playbook: gitlab.yml

