{
  "token_ttl": "{{ token_ttl }}",
  "token_max_ttl": "{{ max_token_ttl }}",
  "policies": [
    "{{ approle_name }}"
  ],
  "bound_cidr_list": [
    "{{ bound_cidr }}"
  ],
  "period": 0,
  "bind_secret_id": true
}
